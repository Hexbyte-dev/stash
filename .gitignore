# Dependencies (installed by npm install during build)
node_modules/

# Build output — committed to avoid using Netlify build minutes.
# Run `npm run build` locally after editing app.jsx, then commit app.js.
